<div class="container">
  <div class="text-center">
    <h1>Add a review for <%= @booking.cat.name %></h1>
    <% if @booking.cat.photo? %>
      <%= cl_image_tag @booking.cat.photo.path, width: 200, radius: 'max', class: "sm-padded bottom-space" %>
    <% end %>
  </div>
  <%= render "shared/review_form" %>
</div>
