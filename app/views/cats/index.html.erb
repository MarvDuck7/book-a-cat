<!-- <div class="container">
  <h1>cats#index</h1>
  <% @cats.each do |cat| %>
  <%= link_to cat.name, cat_path(cat) %>
  <%= cat.description %>
  <%= cat.price %>
  <hr>
  <% end %>
</div> -->

<div class="container">
  <h5 class="bottom-space">Cats at <%= params[:address] %> available on <%= @date %></h5>
  <div class="row">
    <% @cats.each do |cat| %>
      <%= link_to cat_path(cat) do %>
        <div class="col-md-4 col-sm-6 col-xs-12 bottom-space">
          <div class="card" style="background-image: linear-gradient(90deg, rgba(0,0,0,0.4), rgba(0,0,0,0.2), rgba(0,0,0,0.4)), url('<%= cl_image_path cat.photo.path %>');">
            <h2><%= cat.name %></h2>
            <p><%= cat.price %>€/day</p>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
</div>
